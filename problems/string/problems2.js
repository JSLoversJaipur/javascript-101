// Example:2
// Question: Convert the string "hello" to uppercase.
// Explanation: We can use the toUpperCase() method of a string to convert it to uppercase.
const lowercaseString = "hello";
const uppercaseString = lowercaseString.toUpperCase();

console.log("The uppercase string is:", uppercaseString);

// Medium Level Problems

// Check for Anagrams
// Given two strings, check if they are anagrams (contain the same characters, disregarding spaces and punctuation).
